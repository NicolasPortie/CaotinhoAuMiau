@model List<CaotinhoAuMiau.Models.ViewModels.Admin.ColaboradorViewModel>
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Gerenciamento de Colaboradores";
    
    var cargoUsuario = User.ObterValorClaim("Cargo");
    var podeGerenciarColaboradores = cargoUsuario == "Administrador";
    
    var ativos = Model.Count(c => c.Ativo);
    var inativos = Model.Count(c => !c.Ativo);
}

@section Styles {
    <link rel="stylesheet" href="~/css/admin/admin-gerenciamento-colaboradores.css" asp-append-version="true" />
    
    <style>
        
        .modal {
            z-index: 1050 !important;
        }
        .modal-backdrop {
            z-index: 1040 !important;
        }
        .barra-lateral {
            z-index: 100 !important;
        }
        .modal-open {
            overflow: auto !important;
            padding-right: 0 !important;
        }
        body.modal-open .conteudo-principal {
            filter: none !important;
        }
        
        
        body {
            font-family: 'Poppins', sans-serif;
        }
        
        .cartao-status .numero {
            font-family: 'Poppins', sans-serif;
            font-weight: 700;
        }
        
        .cartao-status .rotulo {
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
        }
        
        .modal-title {
            font-family: 'Poppins', sans-serif !important;
            font-weight: 700 !important;
        }
        
        
    </style>
}
        <div class="cabecalho-painel">
            <h1 class="titulo-pagina">Gerenciamento de Colaboradores</h1>
            @if (podeGerenciarColaboradores)
            {
                <button id="btn-novo-colaborador" class="btn-primary" onclick="abrirModalColaborador('criar')" data-bs-toggle="modal" data-bs-target="#modal-colaborador">
                    <i class="fas fa-plus me-2"></i>
                    Novo Colaborador
                </button>
            }
        </div>

        
        <div class="cartoes-status animate-entrada">
            <div class="cartao-status total delay-1">
                <div class="conteudo-cartao-status">
                    <div class="icone">
                    <i class="fas fa-users-cog"></i>
                </div>
                    <div class="d-flex flex-column align-items-start text-start">
                        <div class="rotulo">Total de Colaboradores</div>
                        <div class="numero">@ViewBag.TotalItens</div>
                    </div>
                </div>
            </div>
            <div class="cartao-status ativos delay-2">
                <div class="conteudo-cartao-status">
                    <div class="icone">
                    <i class="fas fa-user-check"></i>
                </div>
                    <div class="d-flex flex-column align-items-start text-start">
                        <div class="rotulo">Ativos</div>
                        <div class="numero">@ativos</div>
                    </div>
                </div>
            </div>
            <div class="cartao-status inativos delay-3">
                <div class="conteudo-cartao-status">
                    <div class="icone">
                    <i class="fas fa-user-slash"></i>
                </div>
                    <div class="d-flex flex-column align-items-start text-start">
                        <div class="rotulo">Inativos</div>
                        <div class="numero">@inativos</div>
                    </div>
                </div>
            </div>
        </div>

        
        <div class="secao-filtros animate-entrada delay-1">
            <div class="row align-items-center">
                <div class="col-md-3 mb-2 mb-md-0">
                    <div class="caixa-pesquisa">
                        <i class="fas fa-search"></i>
                        <input type="text" id="search-input" class="form-control" placeholder="Buscar por nome, email ou CPF...">
                    </div>
                </div>
                <div class="col-md-3 mb-2 mb-md-0">
                    <select id="filter-status" class="form-select">
                        <option value="">Todos os status</option>
                        <option value="ativo">Ativo</option>
                        <option value="inativo">Inativo</option>
                    </select>
                </div>
                <div class="col-md-3 mb-2 mb-md-0">
                    <select id="filter-role" class="form-select">
                        <option value="">Todos os cargos</option>
                    </select>
                </div>
                <div class="col-md-3 d-flex align-items-center gap-2">
                    <button id="btn-filtrar" class="btn btn-primary">
                        <i class="fas fa-filter me-1"></i> Filtrar
                    </button>
                    <button id="btn-limpar-filtros" class="btn btn-secondary">
                        <i class="fas fa-undo me-1"></i> Limpar
                    </button>
                </div>
            </div>
        </div>

        
        <div class="card shadow-sm rounded-4 border-0 mb-4">
            <div class="card-body p-0">
                <div class="tabela-responsiva">
                    <table class="tabela-admin">
                        <thead>
                            <tr>
                                <th scope="col">Nome</th>
                                <th scope="col">Email</th>
                                <th scope="col">CPF</th>
                                <th scope="col">Cargo</th>
                                <th scope="col">Status</th>
                                <th scope="col">Data Cadastro</th>
                                <th scope="col">Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var colaborador in Model)
                            {
                                <tr>
                                    <td>@colaborador.Nome</td>
                                    <td>@colaborador.Email</td>
                                    <td>
                                        @{
                                            string cpfFormatado = colaborador.CPF.Length == 11 
                                                ? colaborador.CPF.Substring(0,3) + "." + colaborador.CPF.Substring(3,3) + "." + colaborador.CPF.Substring(6,3) + "-" + colaborador.CPF.Substring(9,2) 
                                                : colaborador.CPF;
                                        }
                                        @cpfFormatado
                                    </td>
                                    <td>@colaborador.Cargo</td>
                                    <td>
                                        @if (colaborador.Ativo)
                                        {
                                            <span class="indicador-status ativo">Ativo</span>
                                        }
                                        else
                                        {
                                            <span class="indicador-status inativo">Inativo</span>
                                        }
                                    </td>
                                    <td>@colaborador.DataCadastro.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        @if (colaborador.Id != 1)
                                        {
                                            @if (podeGerenciarColaboradores)
                                            {
                                                <div class="grupo-acoes">
                                                    <button class="botao-acao-tabela editar" onclick="editarColaborador(@colaborador.Id)" title="Editar">
                                                        <i class="fas fa-pencil-alt"></i>
                                                    </button>
                                                    <button class="botao-acao-tabela visualizar" onclick="visualizarDetalhesColaborador(@colaborador.Id)" title="Ver Detalhes">
                                                        <i class="fas fa-eye"></i>
                                                    </button>
                                                    @if (colaborador.Ativo)
                                                    {
                                                        <button class="botao-acao-tabela desativar" onclick="abrirModalStatus(@colaborador.Id, '@colaborador.Nome', 'desativar')" title="Desativar">
                                                            <i class="fas fa-user-slash"></i>
                                                        </button>
                                                    }
                                                    else
                                                    {
                                                        <button class="botao-acao-tabela ativar" onclick="abrirModalStatus(@colaborador.Id, '@colaborador.Nome', 'ativar')" title="Ativar">
                                                            <i class="fas fa-user-check"></i>
                                                        </button>
                                                    }
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="grupo-acoes">
                                                    <button class="botao-acao-tabela visualizar" onclick="visualizarDetalhesColaborador(@colaborador.Id)" title="Ver Detalhes">
                                                        <i class="fas fa-eye"></i>
                                                    </button>
                                                </div>
                                            }
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    
                    @if (ViewBag.TotalPaginas > 1)
                    {
                        <div class="paginacao-container w-100" style="margin-top: 2rem;">
                            <div class="paginacao mt-4">
                                <nav aria-label="Navegação de páginas">
                                    <ul class="pagination justify-content-center flex-wrap">
                                        @if (ViewBag.PaginaAtual > 1)
                                        {
                                            <li class="page-item">
                                                <a class="page-link" href="@Url.Action("Listar", "GerenciamentoColaboradores", new { pagina = ViewBag.PaginaAtual - 1, itensPorPagina = ViewBag.ItensPorPagina })">
                                                    <i class="fas fa-chevron-left"></i>
                                                </a>
                                            </li>
                                        }
                                        else
                                        {
                                            <li class="page-item disabled">
                                                <span class="page-link"><i class="fas fa-chevron-left"></i></span>
                                            </li>
                                        }

                                        @{int inicio = Math.Max(1, ViewBag.PaginaAtual - 2);
                                          int fim = Math.Min(ViewBag.TotalPaginas, ViewBag.PaginaAtual + 2);
                                          if (fim - inicio + 1 < 5 && ViewBag.TotalPaginas >= 5)
                                          {
                                              if (ViewBag.PaginaAtual <= 3)
                                              {
                                                  fim = Math.Min(5, ViewBag.TotalPaginas);
                                              }
                                              else
                                              {
                                                  inicio = Math.Max(1, ViewBag.TotalPaginas - 4);
                                              }
                                          }}
                                        @for (int i = inicio; i <= fim; i++)
                                        {
                                            <li class="page-item @(i == ViewBag.PaginaAtual ? "active" : "")">
                                                <a class="page-link" href="@Url.Action("Listar", "GerenciamentoColaboradores", new { pagina = i, itensPorPagina = ViewBag.ItensPorPagina })">@i</a>
                                            </li>
                                        }

                                        @if (ViewBag.PaginaAtual < ViewBag.TotalPaginas)
                                        {
                                            <li class="page-item">
                                                <a class="page-link" href="@Url.Action("Listar", "GerenciamentoColaboradores", new { pagina = ViewBag.PaginaAtual + 1, itensPorPagina = ViewBag.ItensPorPagina })">
                                                    <i class="fas fa-chevron-right"></i>
                                                </a>
                                            </li>
                                        }
                                        else
                                        {
                                            <li class="page-item disabled">
                                                <span class="page-link"><i class="fas fa-chevron-right"></i></span>
                                            </li>
                                        }
                                    </ul>
                                </nav>
                            </div>
                            
                            <div class="paginacao-info mt-3 d-flex justify-content-between align-items-center">
                                <div class="info-paginacao">
                                    Exibindo <span>@Model.Count()</span> de <span>@ViewBag.TotalItens</span> colaboradores
                                </div>
                                <div class="itens-por-pagina">
                                    <label for="selectItensPorPagina">Itens por página:</label>
                                    <select id="selectItensPorPagina" class="form-select form-select-sm">
                                        @foreach (var opcao in new[] { 10, 20, 50 })
                                        {
                                            if (ViewBag.ItensPorPagina == opcao)
                                            {
                                                <option value="@opcao" selected>@opcao</option>
                                            }
                                            else
                                            {
                                                <option value="@opcao">@opcao</option>
                                            }
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>

    
    <div class="modal fade" id="modal-colaborador" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="modal-title" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="modal-header bg-gradient-primary text-white py-4 position-relative">
                    <div class="ms-5 ps-2">
                        <h5 class="modal-title text-white fw-bold mb-1" id="modal-title">Novo Colaborador</h5>
                        <p class="text-white-50 mb-0 small">Preencha os dados para adicionar um novo colaborador ao sistema</p>
                    </div>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
                </div>
                <div class="modal-body p-4">
                    <form id="form-colaborador" action="/Admin/Colaboradores/CadastrarColaborador" method="post" novalidate>
                        @Html.AntiForgeryToken()
                        <input type="hidden" id="employee-id" name="Id" value="0">
                        
                        <div class="card border-0 shadow-sm rounded-4 mb-4">
                            <div class="card-header bg-light border-0 py-3">
                                <h6 class="card-title mb-0"><i class="fas fa-info-circle me-2 text-primary"></i>Informações Pessoais</h6>
                            </div>
                            <div class="card-body p-4">
                                <div class="row g-4">
                                    <div class="col-md-6">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-user text-primary"></i>
                                            </div>
                                            <input type="text" class="form-control ps-5" id="employee-name" name="Nome" placeholder="Nome completo" required>
                                            <label for="employee-name" class="ps-5">Nome Completo</label>
                                            <div class="mensagem-erro" id="employee-name-error"></div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-envelope text-primary"></i>
                                            </div>
                                            <input type="email" class="form-control ps-5" id="employee-email" name="Email" placeholder="email@exemplo.com" required>
                                            <label for="employee-email" class="ps-5">Email</label>
                                            <div class="mensagem-erro" id="employee-email-error"></div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-id-card text-primary"></i>
                                            </div>
                                            <input type="text" class="form-control ps-5 cpf-mask" id="employee-cpf" name="CPF" placeholder="000.000.000-00" required>
                                            <label for="employee-cpf" class="ps-5">CPF</label>
                                            <div class="mensagem-erro" id="employee-cpf-error"></div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-phone text-primary"></i>
                                            </div>
                                            <input type="text" class="form-control ps-5 phone-mask" id="employee-phone" name="Telefone" placeholder="(00) 00000-0000" required>
                                            <label for="employee-phone" class="ps-5">Telefone</label>
                                            <div class="mensagem-erro" id="employee-phone-error"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="card border-0 shadow-sm rounded-4 mb-4">
                            <div class="card-header bg-light border-0 py-3">
                                <h6 class="card-title mb-0"><i class="fas fa-briefcase me-2 text-primary"></i>Informações Funcionais</h6>
                            </div>
                            <div class="card-body p-4">
                                <div class="row g-4">
                                    <div class="col-12">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-briefcase text-primary"></i>
                                            </div>
                                            <select class="form-control ps-5" id="cargoColaborador" name="Cargo" required>
                                                <option value="">Selecione o cargo</option>
                                                <option value="Administrador">Administrador</option>
                                                <option value="Colaborador">Colaborador</option>
                                                <option value="Voluntário">Voluntário</option>
                                            </select>
                                            <label for="cargoColaborador" class="ps-5">Cargo</label>
                                            <div class="mensagem-erro" id="cargoColaborador-erro"></div>
                                        </div>
                                    </div>
                                    <input type="hidden" id="ativoColaborador" name="Ativo" value="true">
                                </div>
                            </div>
                        </div>
                        
                        <div class="card border-0 shadow-sm rounded-4 mb-4">
                            <div class="card-header bg-light border-0 py-3">
                                <h6 class="card-title mb-0"><i class="fas fa-lock me-2 text-primary"></i>Credenciais de Acesso</h6>
                            </div>
                            <div class="card-body p-4">
                                <div class="row g-4">
                                    <div class="col-md-6" id="containerSenha">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-lock text-primary"></i>
                                            </div>
                                            <input type="password" class="form-control ps-5" id="senhaColaborador" name="Senha"
                                                   placeholder="Nova senha (deixe vazio para manter atual)" minlength="6" autocomplete="new-password"
                                                   aria-describedby="senhaColaborador-erro">
                                            <label for="senhaColaborador" class="ps-5">Nova Senha (opcional)</label>
                                            <button type="button" class="toggle-password" title="Mostrar senha" onclick="alternarVisibilidadeSenha('senhaColaborador')">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <div class="mensagem-erro" id="senhaColaborador-erro"></div>
                                        </div>
                                    </div>
                                    <div class="col-md-6" id="linhaConfirmarSenha">
                                        <div class="form-floating position-relative admin-form-group">
                                            <div class="field-icon-container">
                                                <i class="fas fa-lock text-primary"></i>
                                            </div>
                                            <input type="password" class="form-control ps-5" id="confirmarSenhaColaborador" name="ConfirmarSenha"
                                                   placeholder="Confirmar nova senha" minlength="6" autocomplete="new-password"
                                                   aria-describedby="confirmarSenhaColaborador-erro">
                                            <label for="confirmarSenhaColaborador" class="ps-5">Confirmar Nova Senha</label>
                                            <button type="button" class="toggle-password" title="Mostrar senha" onclick="alternarVisibilidadeSenha('confirmarSenhaColaborador')">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <div class="mensagem-erro" id="confirmarSenhaColaborador-erro"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="modal-footer">
                            <button type="button" id="botaoCancelarColaborador" class="botao-contorno-cinza rounded-pill px-4 py-2 botao-modo-edicao" data-bs-dismiss="modal">
                                <i class="fas fa-times me-2"></i>
                                Cancelar
                            </button>
                            <button type="submit" id="botaoSalvarColaborador" class="botao-acao-rapida rounded-pill px-4 py-2 botao-modo-edicao">
                                <i class="fas fa-save me-2"></i>
                                Salvar
                            </button>
                            <button type="button" id="botaoFecharColaborador" class="botao-secundario rounded-pill px-4 py-2 botao-modo-visualizacao" data-bs-dismiss="modal" style="display: none;">
                                <i class="fas fa-times me-2"></i>
                                Fechar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
    
    <div class="modal fade" id="modalConfirmacao" data-bs-backdrop="static" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow">
                <div class="modal-header p-4" id="cabecalhoModalConfirmacao">
                    <h5 class="modal-title fw-bold"></h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
                </div>
                <div class="modal-body text-center p-4">
                    <div class="circulo-icone mb-3">
                        <i class="fas fa-question"></i>
                    </div>
                    <h5 class="fw-bold mb-3" id="tituloConfirmacao"></h5>
                    <p class="mb-4" id="mensagemConfirmacao"></p>
                    <p class="mb-3">Você está prestes a <span id="employee-nameStatus" class="fw-bold"></span>.</p>
                    <input type="hidden" id="idColaboradorConfirmacao" value="">
                    <input type="hidden" id="acaoConfirmacao" value="">
                </div>
                <div class="modal-footer p-3">
                    <button type="button" class="botao-secundario" data-bs-dismiss="modal">
                        <i class="fas fa-times me-2"></i>
                        Cancelar
                    </button>
                    <button type="button" id="botaoConfirmarAcao" class="botao-acao-rapida">
                        <i class="fas fa-check me-2"></i>
                        Confirmar
                    </button>
                </div>
            </div>
        </div>
    </div>

    
    <div class="modal fade" id="modalStatus" data-bs-backdrop="static" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="fas fa-user-check me-2"></i><span id="tituloStatus">Confirmar Ação</span></h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
                </div>
                <div class="modal-body text-center">
                    <h4 class="mb-3" id="subtituloStatus">Tem certeza?</h4>
                    <p class="mb-3">Você está prestes a desativar o colaborador <span id="nomeColaboradorStatus" class="fw-bold"></span>.</p>
                    <p class="text-muted mb-0"><i class="fas fa-info-circle me-1"></i>Essa ação pode ser revertida.</p>
                    <input type="hidden" id="idStatus" value="">
                </div>
                <div class="modal-footer justify-content-center">
                    <button type="button" class="botao-contorno-secundario" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="botao-perigo" id="botaoConfirmarStatus"><i class="fas fa-user-check me-2"></i>Confirmar</button>
                </div>
            </div>
        </div>
    </div>

    

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script src="~/lib/vanilla-masker/vanilla-masker.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.16/jquery.mask.min.js"></script>
    <script src="~/js/admin/gerenciamento-colaboradores.js" asp-append-version="true"></script>
    
    <script>
        $(document).ready(function() {
            // Prevenir que os links de paginação façam o conteúdo desaparecer
            $('.pagination .page-link').on('click', function(e) {
                e.preventDefault(); // Previne o comportamento padrão do link
                var url = $(this).attr('href');
                if (url) {
                    window.location.href = url; // Navega para a URL mantendo o comportamento normal
                }
            });
            
            // Manipulador para o seletor de itens por página
            $('#selectItensPorPagina').change(function() {
                var itensPorPagina = $(this).val();
                var statusFiltro = $('#filter-status').val() || '';
                var cargoFiltro = $('#filter-role').val() || '';
                var pesquisa = $('#search-input').val() || '';
                
                var url = '@Url.Action("Listar", "GerenciamentoColaboradores")' + 
                    '?pagina=1&itensPorPagina=' + itensPorPagina;
                
                if (statusFiltro) {
                    url += '&status=' + encodeURIComponent(statusFiltro);
                }
                
                if (cargoFiltro) {
                    url += '&cargo=' + encodeURIComponent(cargoFiltro);
                }
                
                if (pesquisa) {
                    url += '&pesquisa=' + encodeURIComponent(pesquisa);
                }
                
                window.location.href = url;
            });
            
            // Aplicar filtros quando mudar os campos de filtro
            $('#filter-status, #filter-role').change(function() {
                aplicarFiltros();
            });
            
            // Aplicar filtros ao pressionar Enter no campo de pesquisa
            $('#search-input').keypress(function(e) {
                if (e.which === 13) {
                    aplicarFiltros();
                    return false;
                }
            });
            
            // Botão Filtrar
            $('#btn-filtrar').click(function() {
                aplicarFiltros();
            });

            // Limpar filtros
            $('#btn-limpar-filtros').click(function() {
                window.location.href = '@Url.Action("Listar", "GerenciamentoColaboradores")';
            });
            
            function aplicarFiltros() {
                var statusFiltro = $('#filter-status').val();
                var cargoFiltro = $('#filter-role').val();
                var pesquisa = $('#search-input').val();
                
                var url = '@Url.Action("Listar", "GerenciamentoColaboradores")' + 
                    '?pagina=1&itensPorPagina=' + @ViewBag.ItensPorPagina;
                
                if (statusFiltro) {
                    url += '&status=' + encodeURIComponent(statusFiltro);
                }
                
                if (cargoFiltro) {
                    url += '&cargo=' + encodeURIComponent(cargoFiltro);
                }
                
                if (pesquisa) {
                    url += '&pesquisa=' + encodeURIComponent(pesquisa);
                }
                
                window.location.href = url;
            }
            
            // Atualizar URLs de paginação para incluir filtros
            $('.pagination .page-link').each(function() {
                var href = $(this).attr('href');
                if (href) {
                    var match = href.match(/pagina=(\d+)&itensPorPagina=(\d+)/);
                    if (match) {
                        var pagina = match[1];
                        var itensPorPagina = match[2];
                        $(this).attr('href', getUrlWithFilters(pagina, itensPorPagina));
                    }
                }
            });
            
            function getUrlWithFilters(pagina, itensPorPagina) {
                var statusFiltro = $('#filter-status').val();
                var cargoFiltro = $('#filter-role').val();
                var pesquisa = $('#search-input').val();
                
                var url = '@Url.Action("Listar", "GerenciamentoColaboradores")' + 
                    '?pagina=' + pagina + '&itensPorPagina=' + itensPorPagina;
                
                if (statusFiltro) {
                    url += '&status=' + encodeURIComponent(statusFiltro);
                }
                
                if (cargoFiltro) {
                    url += '&cargo=' + encodeURIComponent(cargoFiltro);
                }
                
                if (pesquisa) {
                    url += '&pesquisa=' + encodeURIComponent(pesquisa);
                }
                
                return url;
            }
            
            // Preencher o select de cargos com os valores disponíveis
            function preencherCargos() {
                var cargos = [];
                
                // Extrair cargos únicos da tabela
                $('.tabela-admin tbody tr').each(function() {
                    var cargo = $(this).find('td:nth-child(4)').text().trim();
                    if (cargo && cargos.indexOf(cargo) === -1) {
                        cargos.push(cargo);
                    }
                });
                
                // Ordenar cargos
                cargos.sort();
                
                // Adicionar opções ao select
                var select = $('#filter-role');
                cargos.forEach(function(cargo) {
                    select.append($('<option>', {
                        value: cargo,
                        text: cargo
                    }));
                });
            }
            
            preencherCargos();

            $('#form-colaborador').on('submit', function(e) {
                e.preventDefault();
                enviarFormulario();
            });

            function enviarFormulario() {
                var form = $('#form-colaborador');
                $.post(form.attr('action'), form.serialize())
                    .done(function(res) {
                        if (res.sucesso) {
                            location.reload();
                        } else {
                            toastr.error(res.mensagem);
                        }
                    })
                    .fail(function() { toastr.error('Erro ao salvar colaborador.'); });
            }

            $('#botaoConfirmarPromocao').on('click', function() {
                var usuarioId = $('#usuarioPromocaoId').val();
                var cargo = $('#cargoColaborador').val();
                var token = $('input[name="__RequestVerificationToken"]').val();

                $.post('/Admin/Colaboradores/PromoverUsuario', { usuarioId: usuarioId, cargo: cargo, __RequestVerificationToken: token })
                    .done(function(res) {
                        if (res.sucesso) {
                            $('#modalPromover').modal('hide');
                            location.reload();
                        } else {
                            toastr.error(res.mensagem);
                        }
                    })
                    .fail(function() { toastr.error('Erro ao promover usuário.'); });
            });
        });
    </script>
}
